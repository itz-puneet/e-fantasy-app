# React Native / Expo projects have many overlapping "peer dependency" ranges.
# This tells npm to install anyway instead of erroring on strict peer conflicts
# (the Expo SDK already guarantees the versions work together). Without this,
# plain `npm install` can fail with ERESOLVE errors.
legacy-peer-deps=true
